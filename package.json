{
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0"
  },
  "scripts": {
    "dev": "npm run rebuild:serve",
    "build:site": "node scripts/integrate-static.mjs",
    "serve:site": "node server/server.js",
    "rebuild:serve": "npm run build:site && npm run serve:site"
  },
  "devDependencies": {
    "cheerio": "^1.0.0",
    "execa": "^9.3.0",
    "express": "^4.19.2",
    "fs-extra": "^11.2.0",
    "globby": "^14.0.0",
    "serve-static": "^1.15.0"
  }
}
